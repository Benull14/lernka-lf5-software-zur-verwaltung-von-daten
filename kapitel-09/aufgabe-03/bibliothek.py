from typing import Callable


def zaehle_wenn(zahlen: list[float], bedingung: Callable[[float], bool]) -> int:
    """Zaehlt, wie viele Werte in der Liste die uebergebene Bedingung erfuellen.

    Gibt die Anzahl der Werte zurueck, fuer die 'bedingung(wert)' True ergibt.
    """
    raise NotImplementedError("TODO")
